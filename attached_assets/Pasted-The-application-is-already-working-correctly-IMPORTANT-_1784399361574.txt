The application is already working correctly.

IMPORTANT: Do NOT refactor, redesign, optimize, rename, or modify any unrelated code.

The ONLY task is to fix the language switching on three pages.

Current issue

The language selector works, but these pages remain in English after selecting Telugu or Hindi:

Report Emergency
Shelters
Flood Alerts

Other parts of the application are already working correctly.

Required Fix

Implement proper localization so that when the user changes the language from Settings or the Language Selector:

English → entire page appears in English
Telugu → entire page appears in Telugu
Hindi → entire page appears in Hindi

The change should happen immediately without requiring a page refresh.

Translate ALL visible UI text

For these three pages, every user-visible string must come from the existing translation system.

Examples include (not limited to):

Report Emergency
page title
subtitle
upload image
take photo
start recording
stop recording
recording...
generate summary
generating...
AI Summary
Recommended Action
Flood Risk Assessment
Severity
Water Level
Immediate Risks
Rescue Advice
Generate SOS
Success messages
Error messages
Empty states
Validation messages
Buttons
Labels
Placeholders
Loading text
Shelters

Translate:

page title
Nearest Shelters
Search
Search placeholder
District filter
Capacity filter
Open
Filling Up
Full
Directions
Call
Distance
Capacity
Beds free
Contact
Tap a pin to view details
results
Loading text
Empty states
Error text
Filter labels

Everything visible on this page must switch language.

Flood Alerts

Translate:

page title
alert cards
severity labels
warning messages
timestamps
evacuation instructions
safety instructions
buttons
loading text
no alerts
refresh
all filter labels
every visible UI string
Dynamic AI Content

Do NOT translate AI-generated responses.

Examples:

AI flood summary
AI image analysis
AI recommendations

Those are already generated in the user's selected language by the backend.

Leave them exactly as they are.

Only translate the application's interface.

Existing Translation System

Reuse the existing:

LanguageContext
translation files
useLanguage hook
existing i18n architecture

Do NOT create a second localization system.

Do NOT hardcode translations inside components.

Every visible string must come from translation keys.

Preserve Everything Else

Do NOT change:

layout
spacing
UI
colors
typography
responsiveness
animations
icons
routing
APIs
backend
AI models
map
shelter logic
report logic
flood alert logic
business logic
voice features
image analysis
SOS
Groq integration

Do not update dependencies.

Do not rename files.

Do not move components.

Do not modify any page except what is required for localization.

Verification Checklist

Before finishing, verify:

✅ Language changes instantly without refresh.

✅ Report Emergency page is fully translated.

✅ Shelters page is fully translated.

✅ Flood Alerts page is fully translated.

✅ English works.

✅ Telugu works.

✅ Hindi works.

✅ Dynamic AI responses remain in the language generated by the backend.

✅ No UI changes.

✅ No layout changes.

✅ No responsiveness changes.

✅ No functionality changed outside localization.

Only fix the localization issue and leave everything else exactly as it is.