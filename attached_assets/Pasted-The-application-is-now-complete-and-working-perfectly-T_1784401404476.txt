The application is now complete and working perfectly.

There is ONLY ONE remaining issue.

On the Shelters page, the FIRST THREE SUMMARY CARDS at the top of the page are not fully responsive on mobile devices.

Current issue:
- On small screens (mobile), these cards overflow horizontally.
- Long text gets cut off.
- The cards do not fully fit inside the viewport.
- Some content extends beyond the right edge of the screen.

I ONLY want these three summary cards to become fully responsive.

Requirements:

1. Modify ONLY the first three summary cards on the Shelters page.
2. Do NOT touch any other cards on this page.
3. Do NOT change any other page in the application.
4. Do NOT redesign the UI.
5. Keep exactly the same:
   - colors
   - typography
   - spacing
   - icons
   - shadows
   - borders
   - gradients
   - animations
   - business logic
   - APIs
   - translations
   - filters
   - map
   - shelter cards below
   - responsiveness of other sections
6. Only improve the responsive layout of these three cards.

Mobile Requirements:

- Cards must fit completely within the screen width.
- No horizontal scrolling.
- No content should overflow outside the card.
- Long translated Telugu and Hindi text must wrap naturally instead of overflowing.
- Numbers (e.g. 8,083 / 19,420) should remain fully visible.
- Progress bars should remain inside the card.
- Icons should stay aligned.
- Padding should scale appropriately for small screens.
- Font sizes may reduce slightly ONLY on mobile if necessary to prevent overflow.
- Cards should maintain equal width.
- Cards should stack vertically with consistent spacing.
- Maintain the existing visual design.

Desktop & Tablet Requirements:

- Desktop layout must remain EXACTLY as it is today.
- Tablet layout must remain EXACTLY as it is today.
- Only adjust the mobile breakpoint.

Please verify using:
- 320px width
- 360px width
- 375px width
- 390px width
- 412px width
- 430px width

After the fix:
✅ No horizontal overflow
✅ No clipped text
✅ No clipped numbers
✅ No layout shift
✅ No visual redesign
✅ Only the first three summary cards changed
✅ Everything else in the application remains exactly the same